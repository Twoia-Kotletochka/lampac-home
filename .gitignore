# секреты и локальные данные — не коммитим
lampac-docker/config/passwd
lampac-docker/database/
lampac-docker/uatv-refresh.log
storage/
lampac-image.tar
lampac-image.tar.gz
*.log

# macOS
.DS_Store

# внешний доступ: секреты и то, что генерируется при установке
edge/.env
edge/creds.txt
edge/hash.txt
edge/Caddyfile
edge/caddy-data/
edge/caddy-config/
